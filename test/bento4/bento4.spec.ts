import 'mocha';
import { expect } from 'chai';
import { Bento4 } from '../../src/bento4/bento4';

describe('Bento4', () => {
    it('should successfully fragment an mp4 asset', () => {
        // Bento4.mp4fragment();
    });

    it('should reject fragmenting if it does not have an mp4 extension', () => {
        // Bento4.mp4fragment()
    });
});
